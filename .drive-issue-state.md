# drive-issue state — #3245

- **Issue**: #3245 — query-semantics-oracle stale-root fixture resolution (AC1/AC2) +
  `fetch-datasets.sh` destroying committed fixtures (AC3/AC4)
- **Routing**: oracle-driven (issue + pinned test, NO OpenSpec change)
- **Agent**: `claude-worker-ip-172-31-63-186`
- **Machine**: ip-172-31-63-186
- **Claim**: `refs/claims/issue-3245` ADOPTED (empty-lease resume of a legacy branch-lock;
  sha 5045503b9e88a82d3fa97105400af7476bcd5690)
- **Branch**: `issue-3245-fetch-corpus-safety` (pushed) — base `origin/main` a116b6b0
- **Worktree**: `/home/ubuntu/workspace/cqlite-wt/issue-3245`
- **Root checkout**: `/home/ubuntu/workspace/repo` (on `main`, left untouched)
- **Board**: `Status=In Progress`
- **Stale ref left for the lead**: `refs/heads/issue-3245-fixture-root-resolution` @ 7e472829
  (pre-squash history, zero #3245 work, no PR) — NOT mine to delete.

## Effective scope (owner triage 2026-08-03T14:43Z + observation 17:48Z)

1. AC1/AC2 = **verify-then-close**, already fixed by #3220 / PR #3236 — record evidence, do not
   re-implement.
2. AC3/AC4 = **re-derive first**. The `rm -rf "${DATASET_ROOT}"` path appears bracketed by the
   #2878 capture-and-restore guard; the issue's evidence may predate it. Fix only a defect that
   still reproduces. AC4's regression check is mandatory per the owner.

## Env facts (this box IS the reproduction environment)

- `/data/datasets/sstables/test_da/` exists, has **no** `multiclustering_table-*`.
- In-repo corpus HAS `test_da/multiclustering_table-fd74ad508d2311f1a29b6d2c15dcffdf` (#3032).
- `/data/datasets` also lacks `commitlog/`, which the repo tracks — the AC3 destruction surface.
- Runs use `CQLITE_DATASETS_ROOT=/data/datasets`. Probe non-destructively with
  `fetch-datasets.sh --verify-only`; NEVER the default destructive path against a real corpus.
- git auth needed wiring on this box: `credential.https://github.com.helper = !gh auth git-credential`.

## Stage log

- 2026-08-04T20:37Z — claim adopted, worktree+branch created and pushed, heartbeat beaten,
  board → In Progress, coord announcement posted
  (issues/3245#issuecomment-5184351403).
- 2026-08-04T20:45Z — STAGE: **verify-before-fix**. Two verification subagents dispatched
  (AC1/AC2 empirical run; AC3/AC4 read-only census). No open coord request. No cron armed.
- 2026-08-04T20:55Z — AC3/AC4 census COMPLETE. Verdict:
  - **AC3 reported defect: already fixed by #2878.** `rm -rf` is now `F:1145`, bracketed by
    capture `F:1135` / `DESTRUCTIVE_STARTED=1 F:1143` / `restore all || exit 1 F:1171`. Crash +
    signal paths covered (traps `F:1082`, `F:1086-1088`). Issue's `F:222` cite predates the guard.
    **Do NOT re-fix the rm -rf.**
  - **Three residuals**: (a) unstaged local modifications to tracked fixtures silently reverted
    (restore rewrites INDEX content `F:848-851`; no pre-`rm` dirty check anywhere) — DATA LOSS
    reported as success; (b) SIGKILL window repaired only by a later non-`--verify-only` run;
    (c) exit postcondition is `git diff` (`F:865`), so a STAGED deletion is invisible — AC3's
    literal `git status --porcelain` is never asserted.
  - **AC4: SATISFIED** by `scripts/tests/test_fetch_datasets_tracked_guard.sh:355-384`
    (mutation-proven, gate component `tooling-tests`, `scripts/agent-gate.sh:5564`). Caveat: on
    no PR-visible tier, so `required` does not pin it.
- 2026-08-04T20:58Z — STAGE: **implement**. `fetch-guard-impl` dispatched for residuals (a) + (c)
  ONLY — both are implied by AC3's literal wording + the owner's "a repair procedure that damages
  tracked data is worse than the failure it repairs" principle, so they need no scope ruling.
  Residual (b) and the PR-visible-tier wiring are NOT in this diff (see coord request below).

## MUST DO BEFORE MERGE

- **Delete this file** in a final commit before `flow-closer` merges. It is work-loss insurance for the
  implement/review stages only and must NOT land on `main`. (Kept committed until then deliberately: a
  watchdog-killed session loses everything uncommitted.)
- Lite re-cert must cover the FINAL commit (the recorded PASS was at `823b2d4`; `6c4ac714` postdates it).
- `scripts/tests/test_fetch_datasets_tracked_guard.sh` lives in gate component `tooling-tests`, which
  `--lite` does NOT cover — run it explicitly every round. Verified 225 passed / 0 failed at `6c4ac714`.

## Verification results (2026-08-04T21:0xZ) — posted to the thread

- **AC1 SATISFIED, empirically**: both `multiclustering_bti__*` cases RAN and asserted (10 and 16 semantic
  rows vs 468 physical, both lanes, reconciliation applied) under
  `CQLITE_REQUIRE_FIXTURES=1 CQLITE_DATASETS_ROOT=/data/datasets` — a root lacking the fixture, so it
  resolved via the per-table checkout fallback. All 10 oracle cases ran.
- **TRAP, recorded**: `cargo test --test query_semantics_oracle_parity` WITHOUT
  `--features "state_machine cli-helpers"` compiles an EMPTY binary and reports
  `running 0 tests ... ok` — a vacuous green. The file is `#![cfg(all(...))]` at line 42 and `cli-helpers`
  is non-default. The gate is correct (`agent-gate.sh:5029`); a hand-run is what goes vacuous.
- **AC2 INTACT**: `query_semantics_oracle_parity.rs:747/:756` + per-case guards `:371/:387/:396/:404`.
- **AC3 headline ALREADY GUARDED** by #2878; **AC4 SATISFIED** by mutation-proven
  `test_fetch_datasets_tracked_guard.sh:355-384` in `tooling-tests`.
- **NOT a defect** (checked, do not re-derive): `query-semantics-oracle` absent from `DATASET_COMPONENTS`
  (`agent-gate.sh:7057`) is DELIBERATE and documented at `:5010-5011` — the component self-guards.

## Coord requests posted (lead clears the labels, never me)

- `REQ-3245-20260804T210201Z-claude-worker-ip-172-31-63-186` — **decision**, scope substitution
  (AC3-as-written is not deliverable; delivering residuals (a)+(c) instead). `coord:needs-attention`.
  Default: proceed with option A (land residuals under #3245, close on merge). NOT blocked.
- `REQ-3245-20260804T210201Zb-claude-worker-ip-172-31-63-186` — **follow-up**, residual (b)
  (`--verify-only` cannot diagnose a SIGKILL-orphaned root) + wiring the guard suite onto a PR-visible
  tier. `coord:follow-up-proposed`.

## Open coord request

**To post once AC1/AC2 verification lands** (draft is durable here so a fresh session can post it):
scope disposition for #3245, given the issue is now largely already-fixed.
- Recommendation: close AC1/AC2 as verified-by-#3220 and AC4 as satisfied-by-the-existing
  mutation-proven test; land residuals (a)+(c) under this issue; split residual (b) (`--verify-only`
  should DIAGNOSE a SIGKILL-orphaned root) and the `required`-tier wiring for
  `test_fetch_datasets_tracked_guard.sh` into ONE follow-up (`coord:follow-up-proposed`).
- Why the tier wiring must be a separate PR regardless: `required` evaluates the aggregator and
  `.github/ci-gating-tiers.yml` from the PR's **BASE** ref, so a registry change cannot certify
  itself (CLAUDE.md).
- Default if no answer: proceed exactly as recommended.

## Cron

_none armed_
