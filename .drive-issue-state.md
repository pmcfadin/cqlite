# drive-issue #2605 — durable state

- **Issue**: #2605 — spike(0.16→0.17): DataFusion TableProvider PoC over the flight scan path
- **Routing**: oracle/bench-driven — issue body says explicitly "No OpenSpec (bench-only, no product surface)". NO Seam 1.
- **Claim**: refs/claims/issue-2605 held by machine ip-172-31-7-163
- **Worktree**: /data/lanes/lane-2605
- **Branch**: issue-2605-datafusion-tableprovider-spike (not yet pushed)
- **PR**: none yet
- **Open coord request**: none
- **Stage**: explore (scan-path survey in flight)
- **Notes**: `github-coord` plugin NOT installed on this box — coord protocol followed by hand
  (request marker + coord:* labels). Residual to report.
- **Datasets**: export CQLITE_DATASETS_ROOT=/data/datasets (155 *-Data.db)
- **Updated**: 2026-08-28T06:12Z

## Findings so far (durable)
- **DataFusion pin = 44.0.0** — verified by real resolution: `datafusion 44.0.0` -> `arrow 53.4.1`,
  the EXACT version the workspace + arrow-flight 53.4.1 use. DF 43 wants arrow ^53.2 (also ok);
  DF >=45 jumps to arrow 54 and would FORK the arrow type graph (two RecordBatch types, breaking
  the whole "reuse the existing scan path" premise). So 44.0.0 is the pin.
- Workspace already carries BOTH arrow 53.4.1 and 54.x (54 via some other dep) — check which.
- Dep weight baseline: datafusion-44-alone lock = 228 crates; cqlite workspace lock = 658 crates.
  Implementer must measure the actual workspace delta with the feature ON.
- Machine: 16 cores, 30GB RAM, 261G free on /data. rustc 1.97.1 (pinned).
- Acceptance also governed by throughput-program-2026-07.md L534 (M15): isolate decode-to-column
  vs vectorized-exec delta; wide+overlap shape; peak mem under 512Mi; both arms post-reconciliation.
  Columnar-producer slot trigger: >1.3x on wide/overlap -> revisit; else Stage-3 prep.

## CORPUS DECISION (resolved from acceptance criteria + repo state — NOT escalated)
- Local corpus is USELESS for this bench: biggest *-Data.db under /data/datasets is **0.6 MB**.
  R12 was ~1.9M rows. Benching a vectorized engine on 0.6MB measures startup, not execution.
- The issue says "the R12 corpus **shape** (~1.9M partitions, 2 gens)" and M15 says "a wide/overlap
  corpus" — i.e. a SHAPE is specified, not the literal R12 dataset. So synthesizing a
  shape-matching corpus SATISFIES the criteria; no owner decision needed.
- Basis: `test-data/scripts/gen-perf-corpus-3068.sh` — cassandra-stress driven, multi-GB, REAL
  Cassandra 5.0 nb/BIG bytes (oracle-grade, NOT CQLite-written), already has a `wide_4kb` shape.
- REQUIRED VARIANT: #3068's script ends with a single `nodetool compact` to force ONE sstable
  (it deliberately avoids merge pollution). This spike needs the OPPOSITE — >=2 overlapping
  generations so the k-way merge / reconciliation path actually runs (M15 item 4).
- Docker present + working on this box; no containers running; no pre-existing corpus.
- Disk: 261G free on /data. 16 cores, 30GB RAM.
- Corpus provenance MUST be stated prominently in the spike report: it is NOT the literal R12
  dataset, so absolute rows/s are not directly comparable to R12's 10.6k rows/s/pod figure; the
  RELATIVE engine delta is the load-bearing number.

## In flight (2026-08-28 ~06:25Z)
- Explore agent: mapping cqlite-flight scan path / Arrow batch seam.
- corpus-2605 (sstable-developer): writing test-data/scripts/gen-df-spike-corpus-2605.sh +
  generating /data/corpus-2605 (perf_2605, wide, ~1.9M rows gen1 + ~30% overlapping gen2,
  NO major compact, >=2 Data.db asserted). ~1hr. Instructed NOT to touch any Rust/Cargo/
  cqlite-flight file (concurrent-agent conflict avoidance).
- DF pin decided: datafusion 44.0.0 (arrow 53.4.1 exact match with cqlite-flight).
- arrow 54 in lock comes ONLY from duckdb 1.2.2 (optional cqlite-cli duckdb-tests feature) -
  irrelevant to us.
- cqlite-flight/Cargo.toml: arrow = workspace + ipc; arrow-flight = "53" (NOT workspace-pinned).
  Feature convention: `default = []` with a long WHY comment per feature.
- gen-perf-corpus-3068.sh compact step to AVOID = lines ~346-349.

## SURVEY RESULTS (2026-08-28 ~06:35Z) — existing infra that supersedes a from-scratch build
- **`scripts/perf/ws0-baseline.sh` IS the two-arm rows/s rig** (both arms, same bytes, same core
  pair, rows/s + cycles/row, warm/cold separate, median-of-N + spread, setup subtracted).
  `--corpus /data/ws0-3096 --arm bypass|merge|both --reps N --temp warm|cold|both`.
  Support: scripts/perf/lib-*.sh + ~20 ws0_*.py. EXTEND this, do not reinvent.
- **JSON contract**: tools/ws0-corpus-gen/src/bin/scan_bench.rs (`ws0-scan-bench`) — rows_per_sec,
  setup_secs timed separately, --rows-denominator echoed, `--fold` ANTI-ELISION guard, non-zero
  exit on zero rows. (Anti-elision matters: a folded count(*) = fake vectorized win.)
- **CORPUS AVAILABLE NOW**: /data/ws0-3096 — 2.6GB, ws0.events, seed 30960001, 4,000,000 rows /
  40,000 partitions / 100 rows-per-partition / 12 cells-per-row, nb-1-big-Data.db=2,774,760,422 B,
  693.7 B/row, sha256 recorded. Layout /data/ws0-3096/ws0/events/ + ws0-events.cql.
  CAVEATS: ONE generation; UNCOMPRESSED (CQLite-written).
- /data/datasets is 23MB total; every non-system_schema table = 1 generation; biggest Data.db 647KB.
  UNUSABLE for rows/s. /data/ws0-3299 = outputs only, no corpus.
- **Forced-arm seam**: CQLITE_FLIGHT_MERGE_PATH=bypass|merge (bypass.rs:46-60). ws0-baseline --arm
  sets it (lib-measure.sh:176). `bypass` only PREFERS fast path — actual arm is
  results.json `.executed_merge_path` (ws0_flight_arm.py:55,97-100). READ that field.
- **Post-reconciliation citations** (don't re-derive): StreamingStep::ClusterGroup = "One
  already-reconciled row" (core/.../merge/streaming.rs:209-219); reconcile =
  finalize_current_cluster -> reconcile_cluster_with_overlap_counted -> apply_partition_shadowing
  (streaming.rs:567-597); row tombstone/liveness in MergeProducer::entry_to_row (producer.rs:994)
  per row_source.rs:143-147.
- **Decode-to-column instruments**: cqlite-core/benches/export_throughput.rs = existing STRICT
  perf gate on `export/rows_to_record_batch` (the isolated CQL->Arrow encode cost). Single
  batch-materialization point = MergeProducer::flush_buffer (egress_flush.rs:51-58).
  arrow_convert.rs:147/:279 = rows_to_record_batch{,_with_schema}.
- **Peak-mem precedent**: cqlite-flight/tests/issue_1494_producer_mem_budget.rs under `dhat-heap`
  feature ([[test]] required-features). Use it for the 512Mi budget, not hand-rolled /proc.
- **Pushdown free win**: arrow_schema() already stamps per-field `cqlite:pushdown` metadata =
  "full"|"equality"|"none" from pushdown_capability(&CqlType) (producer.rs:1214). Drive
  supports_filters_pushdown off it.
- ScanSpec = {token, filter, projection, limit} (filter.rs:236); from_ticket (:256).
  LIMIT deliberately enforced DOWNSTREAM only (producer.rs:~790-805).
- produce_streaming + BatchSink + CollectSink + spawn_streaming are pub(crate) => TableProvider
  MUST live in cqlite-flight. produce_streaming_from_readers_to_vec IS pub (producer_warm.rs:209).
- **ORACLE BOUNDARY**: ws0-corpus-gen README:19-30 x3 — "performance fixture only, NEVER a
  correctness oracle" (#3042). Equivalence test MUST use real Cassandra fixtures /data/datasets.

## DECISION: keep the LZ4 Cassandra corpus as HEADLINE (rejecting survey's ws0-corpus-gen advice)
Rationale: CQLite write surface is uncompressed-only (#1406); R12 was LZ4;
PHASE_STREAM_DECOMPRESS is a real sub-phase. An uncompressed corpus understates decode cost and
thus OVERSTATES the vectorized-exec share — corrupting the very number M15 asks to isolate.
=> develop/cross-check on /data/ws0-3096; HEADLINE on /data/corpus-2605 (LZ4, 2 overlapping gens).
Report both, labelled.

## RIG BOUNDARY (probed 06:40Z) — ws0-baseline.sh is corpus-layout-bound
- Reads $CORPUS/ws0-events.cql (lib-inputs.sh:86,148) + expects $CORPUS/ws0/events/, AND validates
  against canonical pin tools/ws0-corpus-gen/src/measurement_corpus.rs; REFUSES divergent corpus.
  => It is a #3096-specific rig, NOT a general two-arm harness.
- PLAN: (a) /data/ws0-3096 -> use rig as-is (dev loop + cross-check, add DF arm).
         (b) /data/corpus-2605 (LZ4, headline) -> thin driver reusing rig LIBRARIES
             (lib-measure/cpu/host-state/perf-lint.sh + ws0_*.py), NOT its corpus-identity
             machinery. Do NOT shim the corpus into a fake ws0/events layout.
- `--non-baseline` MANDATORY for non-canonical corpus; labels manifest+report 'NOT A WS0 BASELINE'.
  Carry that label verbatim into the spike report where applicable.
- Rig discipline to mirror: warm/cold NEVER blended (--scan-passes N>1 refused with cold;
  --cold-step-duration >5000ms refused; ws0_report.py rejects cold rep with requests_ok != 1);
  median-of-N + spread printed; setup subtracted; --out refuses non-empty dir.
- HOST VERIFIED measurement-ready: perf_event_paranoid=-1, kptr_restrict=0, /usr/bin/perf works,
  99-cqlite-perf.conf installed. Sibling pairs: 0,8 1,9 2,10 3,11 4,12 5,13 6,14 7,15.
  Rig defaults (--server-cpus 2,10; --client-cpus 4,12,5,13,6,14,7,15) valid here.
- `--validate-args-only` = arg dry-run, no writes/build/perf. Use before long runs.

## UNIT NORMALIZATION HAZARD (flagged to impl 06:45Z)
Trigger rules are in POD units (~30k rows/s/pod promote threshold; R12 = 10.6k rows/s/pod at
3 pods / 4-vCPU pods / RF=3 / LZ4 / ~1.9M partitions/node). But the rig pins to a PHYSICAL-CORE
SIBLING PAIR (2 hyperthreads of ONE core) != a 4-vCPU pod.
=> rows/s/pod-equivalent is a NORMALIZATION, not a measurement. A 2x factor error flips the
   recommendation across the 30k threshold.
REQUIRED of the report: raw measured rows/s + cycles/row FIRST; pod-equivalent as a clearly
labelled derived figure with the arithmetic inline; labelled as normalization IN THE TABLE;
if no defensible factor, give a CONDITIONAL recommendation ("promote iff 4-vCPU pod >= N
physical cores") rather than a single number on an unstated assumption.
Also: the >1.3x columnar-producer slot trigger must be evaluated on the WIDE/OVERLAP (LZ4) corpus
track specifically, not the ws0-3096 track. State which corpus each trigger used.

## CORPUS REVIEW (06:50Z) — manifest-2605.json v1
GOOD (verified in manifest): LZ4Compressor chunk_length_in_kb=16 + CompressionInfo.db present;
autocompaction disabled, no major compaction; 10,351,934,276 bytes; gen1 190k partitions x 10 rows
= 1.9M rows in 63s; rows_written_total 2,470,000; logical_rows_post_reconciliation 1,900,000;
overlap MEASURED 0.288 via writetime(body) over fixed 1% murmur3 token slice with
slice_rows_after_gen1 == slice_rows_after_gen2 == 19690 (proves OVERWRITE not new keys).
sstable_dir sstables/perf_2605/wide_4kb-0bfa45c0a2a911f19c7b7fcf9962b5ee.

DEFECT 1 (fidelity, sent back): data_db_count = **25**, not 2. Autocompaction-disabled => every
memtable flush left its own SSTable => ~25-way k-way merge. Issue asks "2 gens";
throughput-program-2026-07.md:158 says the 2-gen band is an ASSUMPTION/STCS expected-k band.
BIAS DIRECTION: both arms share the post-reconciliation batches, so a 25-way merge inflates the
SHARED decode+merge floor and SHRINKS the apparent vectorized-exec share => biases toward
"don't promote". Conservative-looking bias = hardest to notice.
FIX ORDERED: compact gen1 to 1 sstable BEFORE loading gen2; then gen2 flush/compact to 1;
assert 2<=count<=3; re-measure overlap after compaction. Keep 25-file corpus as
/data/corpus-2605-k25 = deliberate high-k SECONDARY variant (deep-merge sensitivity point).
DEFECT 2: min/max_timestamp_us garbage (82820260624071787898247892692 = concatenated
sstablemetadata values). Parse or drop - it is a provenance artifact the report cites.
DEFECT 3: declared_replication_factor=3 needs caveat - SimpleStrategy RF=3 single-node stores ONE
replica; RF=3 nominal only. M15 says "wide + RF=3/overlap"; we deliver OVERLAP half only. Must
not imply replication.

## impl-2605 progress
Created cqlite-flight/src/df_spike/, modified cqlite-flight/Cargo.toml + Cargo.lock (datafusion
added + resolved). NOT yet committed as of 06:48Z.

## AC4 DEPENDENCY IMPACT (measured 06:52Z)
Workspace Cargo.lock: 658 -> **695 crates = +37** with datafusion-spike resolved.
datafusion = { version = "44.0.0", optional = true, default-features = false }
datafusion-spike = ["dep:datafusion"]   (non-default; WHY block written, matches crate convention)
arrow stays 53.4.1 (54.2.1 remains, duckdb-only). Pin HOLDS in the real workspace.

## OPEN RISK flagged to impl (06:52Z): default-features = false may UNDERSTATE DataFusion
Stripping DF default features can remove the SQL planner and/or optimizer passes (projection/
filter pushdown, count(*) aggregate handling). Same bias direction as the k=25 corpus defect:
makes DataFusion look WORSE => false "don't promote". Required of the report:
 (1) list exactly which DF features are enabled, in the methodology section;
 (2) say whether the SQL planner is available or plans are hand-built (hand-built plans can
     bypass optimizer passes a real deployment would get);
 (3) if a feature a real #941 deployment would enable is missing, the measured delta is a
     FLOOR, not the delta - and must be labelled "floor", because the trigger is a threshold
     test and understatement produces a false "don't promote";
 (4) if fidelity needs a default feature, ENABLE it and re-measure the +37.
ANTI-ELISION (sharpest for count(*)): DF can satisfy count(*) from statistics/constant-fold
without touching a row => enormous fictional win. Keep ws0-scan-bench `--fold` guard AND assert
the DF arm consumed the expected row count vs manifest logical_rows_post_reconciliation
(1,900,000). DF answering count(*) from metadata = a FINDING to report, not a throughput win.

## IMPL REVIEW (07:00Z) — three choices AFFIRMED, do not re-litigate under review
1. rss.rs samples /proc/self/statm, NOT VmHWM. CORRECT and better than the dhat-heap precedent I
   suggested: VmHWM is process-wide, never decreases => contaminated by earliest-peaking phase,
   cannot attribute peak to a scenario/arm. And dhat measures HEAP while the B4 512Mi budget is an
   RSS budget. Report must state sampling interval + acknowledge sub-interval spikes can be missed.
2. lib.rs:38-39 `#[cfg(feature = "datafusion-spike")] pub mod df_spike;` with WHY block => AC1
   preserved. Still verify AC1 EMPIRICALLY (default build/clippy unaffected; datafusion absent
   from default-features resolution), not by inspection.
3. filter.rs: `fn lower_predicate_expr` -> `pub(crate) fn` (+9/-1). GOOD. Routes translated DF
   filters through the PRODUCTION lowering instead of re-deriving operand coercion => prevents the
   two engines disagreeing about which rows a predicate keeps = the concrete guard against
   "faster because wrong". Lowering FAILURE treated as "not pushable" = right fail-closed default.
   Visibility widening only, zero behavior change => does NOT breach "zero production wiring", but
   MUST be called out in the PR body since a reviewer sees a production file modified.
df_spike/ files: mod, provider, exec, scan, pushdown, rowwise, bench, rss = 2096 lines, largest
320 (all well under the ~800 campsite target).
WORK-LOSS RISK flagged: 2096 lines uncommitted 15min in, with a 10GB Cassandra load + 2 peer
sessions contending for CPU. Told to commit immediately (WIP ok).
Corpus side: dd455873f fixed the sstablemetadata timestamp parse (reads parenthesized micros).

## CORPUS AGENT WENT IDLE WITHOUT REBUILD (07:05Z)
Facts: /data/corpus-2605/manifest-2605.json still the 06:28 run, data_db_count=25.
No /data/corpus-2605-k25 dir. docker ps EMPTY (cqlite-df2605 torn down).
Commits a56397c46 + dd455873f present (timestamp fix landed).
=> k=2 rebuild NOT done. Re-sent the request with rationale + explicit ordering.
Rebuild needs a FRESH container (old one gone). Cost ~15-20min (gen1 load 63s + 8GB compaction).
IF corpus agent cannot deliver k=2: fallback is to bench BOTH k=25 (have it) and ws0-3096 (k=1),
and report the merge-depth sensitivity as a RANGE, stating that the 2-gen expected-k point was
not measured. That is honest but weaker; prefer the rebuild.

## COMMITS (07:10Z)
a56397c46 test(#2605): corpus generator
dd455873f test(#2605): sstablemetadata timestamp parse fix
f7f03d0ab spike(#2605): feature-gated DataFusion TableProvider over the existing Flight scan path
=> impl acted on the commit-now nudge; 2117 lines now SAFE. Work-loss risk closed.

## *** KEY METHODOLOGY FINDING (07:15Z): THREE CONSERVATISMS STACK THE SAME WAY ***
All three independently bias AGAINST DataFusion (i.e. toward "don't promote"):
 1. rowwise.rs is FASTER than the production row engine (production evaluates against QueryRow =
    HashMap<String,Value>, paying per-cell string hash + Value enum construction; the arm
    downcasts once per batch and indexes). => measured DF advantage is a LOWER BOUND.
 2. datafusion default-features = false may strip SQL planner / optimizer passes a real #941
    deployment would enable. => understates DF.
 3. LZ4 corpus currently k=25 not k=2 => inflates SHARED decode+merge floor => shrinks apparent
    vectorized-exec share. => understates DF.
WHY IT MATTERS: the promotion rule is a THRESHOLD test (~30k rows/s/pod; >1.3x columnar slot).
Three stacked understatements can push a real "promote" across into an apparent "don't promote".
REQUIRED of the report: an explicit "direction of bias" subsection naming all three, stating each
errs against DF, and concluding:
 - LARGE measured win => ROBUST "promote" (survived three conservatisms);
 - SMALL measured win => does NOT license a confident "don't promote"; verdict is INCONCLUSIVE
   + named follow-up re-measurements.
 - state which of the three were in force for each reported number.
Prefer "inconclusive with a named follow-up" over a FALSE NEGATIVE on a decision gating an epic.

## Good instincts in f7f03d0ab worth preserving under review
- 4 arms: floor / row_engine / datafusion / row_pushdown (floor = shared production baseline,
  makes the two deltas separable).
- read_path_probe => read-arm evidence (merge vs bypass) as required by M15 item 4.
- pushdown validated via PRODUCTION filter::lower_predicate_expr => Exact never claimed for a
  predicate the scan does not apply.
- rss.rs returns None, never a fabricated 0.
- unsupported filter type = ERROR, not a silently-false comparison (would make row arm look
  arbitrarily fast).
- resident bound stated: CHANNEL_CAPACITY x max_batch_bytes.
- provider/exec: single-partition plan over pre-resolved paths => no blocking discovery, no nested
  runtime inside DataFusion's runtime.
- sub-phase timing read back from the always-compiled #2819 instrument.

## PROGRESS (07:25Z)
Commits (branch PUSHED to origin, head 74b208de9):
  a56397c46 corpus generator
  dd455873f sstablemetadata timestamp parse fix
  f7f03d0ab feature-gated DataFusion TableProvider (4 arms)
  4fb949931 clippy/fmt clean under datafusion-spike (OutcomeSlot alias, matches!)
  74b208de9 make merge depth the shape the issue asks for, and verify LZ4
Working tree CLEAN (all committed).
CORPUS: /data/corpus-2605-k25 preserved + labelled variant="k25-high-merge-depth (SECONDARY:
  deliberate deep-merge sensitivity point, NOT the headline corpus...)". All 3 manifest defects
  FIXED: timestamps now 16-digit micros (1787898247892692); replication_note added ("NOMINAL ONLY.
  SimpleStrategy RF=3 on a single-node container stores exactly ONE replica... delivers the WIDE
  and OVERLAP halves"). Fresh container up, k=2 rebuild IN PROGRESS into /data/corpus-2605.
STILL PENDING: --lite summary block; empirical AC1; which DF features enabled; 512Mi peak RSS;
  bench numbers; spike report; PR.
