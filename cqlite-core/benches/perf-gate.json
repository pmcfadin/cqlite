{
  "_comment": "Performance regression gate policy (Issue #572). The CI workflow .github/workflows/perf-regression.yml runs these benches on both the PR and main on the SAME runner and fails if any STRICT bench's Criterion median regresses by more than its threshold_pct. ADVISORY benches are always reported but never cause a non-zero exit, regardless of delta — they exist to document cost, not to block merges. To adjust, edit bench entries here; see cqlite-core/benches/README.md.",
  "default_threshold_pct": 10,
  "advisory_benches": [
    "write/ingest_wal_on",
    "compaction/wide"
  ],
  "benches": [
    {
      "id": "read/get_partition_big",
      "threshold_pct": 10
    },
    {
      "id": "read/get_partition_bti",
      "threshold_pct": 10
    },
    {
      "id": "read/clustering_slice",
      "threshold_pct": 10
    },
    {
      "id": "read/full_scan",
      "threshold_pct": 10
    },
    {
      "id": "read/type_heavy",
      "threshold_pct": 10
    },
    {
      "id": "write/ingest_wal_off",
      "threshold_pct": 10
    },
    {
      "id": "write/ingest_wal_on",
      "threshold_pct": 25,
      "_note": "advisory — I/O-dominated by fsync; reported but never fails CI"
    },
    {
      "id": "write/flush",
      "threshold_pct": 10
    },
    {
      "id": "read_while_write/readers6_writers2",
      "threshold_pct": 25,
      "_note": "reader-side median under concurrent write load; wide threshold (contention bench); p99 tail owned by A2 tail-latency harness #1563"
    },
    {
      "id": "compaction/narrow",
      "threshold_pct": 10
    },
    {
      "id": "compaction/wide",
      "threshold_pct": 10,
      "_note": "advisory — memory/data-shaped fat-partition merge; O2's dhat budget is its guard, not this wall clock"
    },
    {
      "id": "compaction/tombstone_heavy",
      "threshold_pct": 10
    }
  ],
  "scaling_floors": [
    {
      "id": "concurrent_scan/buffered/n4",
      "baseline_id": "concurrent_scan/buffered/n1",
      "degree_ratio": 4,
      "min_scaling": 1.8,
      "_note": "throughput(n4)/throughput(n1)=degree_ratio*median(n1)/median(n4); healthy≈2.98, serialized≈1.0 → floor 1.8 leaves CI-runner headroom (issue #1564)"
    },
    {
      "id": "concurrent_scan/mmap/n4",
      "baseline_id": "concurrent_scan/mmap/n1",
      "degree_ratio": 4,
      "min_scaling": 1.8,
      "_note": "throughput(n4)/throughput(n1)=degree_ratio*median(n1)/median(n4); healthy≈3.19, serialized≈1.0 → floor 1.8 leaves CI-runner headroom (issue #1564)"
    }
  ]
}
