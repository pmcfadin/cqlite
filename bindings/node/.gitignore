# Build artifacts
*.node
target/

# npm
node_modules/

# Generated by napi-rs at build time; kept for npm publish but not tracked in git.
# These files are regenerated by `npm run build` and included in the published package
# via the `files` array in package.json.
index.js
index.d.ts
