{
  "doc": [
    "Query-semantics parity oracle (issue #1742).",
    "",
    "This file is a DIFFERENT oracle from the physical sstabledump JSONL goldens",
    "(`*-Data.db.jsonl`). The physical goldens enumerate every on-disk cell —",
    "INCLUDING tombstones, deleted rows, and expired-but-uncompacted TTL cells.",
    "Row-count/value parity against a physical dump therefore CANNOT catch a",
    "read-time-reconciliation bug: when CQLite fails to reconcile, both sides still",
    "contain the shadowed/expired rows and parity passes while a real Cassandra",
    "SELECT diverges (issue #1741, the read-path P0 this oracle guards against",
    "regressing).",
    "",
    "Each case below records the POST-RECONCILIATION result set a real Cassandra",
    "`SELECT` returns for a canonical query, evaluated at a PINNED `now`",
    "(`pinned_now_secs`, epoch seconds) so TTL expiry is deterministic and never",
    "wall-clock-flaky. The reader honors the pin via the debug-only",
    "`CQLITE_TTL_NOW_OVERRIDE_SECS` seam (see `now_clock.rs`).",
    "",
    "`physical_row_count` is the count of `type == \"row\"` entries in the committed",
    "sstabledump golden — an anti-empty-pass sanity check AND (where it exceeds",
    "`expected_rows.len()`) a direct demonstration of the physical-vs-semantic gap",
    "this oracle exists to close.",
    "",
    "`physical_static_only_partitions` (additive-optional, default 0; issue #3095)",
    "is the count of golden partitions holding a `static_block` and NO `row` entry.",
    "Cassandra returns exactly ONE result row for each such partition, and that row",
    "has no physical `row` behind it — so it is the ONLY way a correct semantic",
    "result may legitimately EXCEED `physical_row_count`. Both lanes re-derive the",
    "count from the golden and assert this declaration, so the allowance can never",
    "be inflated to mask an over-emitting read path.",
    "",
    "`ddl` (additive-optional) is the keyspace-qualified `CREATE TABLE` the FLIGHT",
    "lane's ticket must carry, required for any table whose shape differs from the",
    "`(id int, ck int, v text, PRIMARY KEY (id, ck))` shape shared by every table",
    "in `compaction-tombstone-ttl-parity.cql` (e.g. a `static` column).",
    "",
    "Consumed by cqlite-core/tests/query_semantics_oracle_parity.rs."
  ],
  "oracle_version": 1,
  "provenance": {
    "cassandra_version": "5.0.2",
    "cassandra_git_sha": "f278f6774fc76465c182041e081982105c3e7dbb",
    "fixture_generation_command": "bash test-data/scripts/generate-compaction-tombstone-ttl-parity.sh",
    "note": [
      "All three fixtures below are REAL Apache Cassandra 5.0.2 SSTables committed",
      "to the repo (test-data/datasets/sstables/test_compaction_tombstone_ttl/**),",
      "with provenance already tracked in test-data/cassandra-parity-manifest.yml",
      "(scenarios cass.compaction.*shadow_row_delete / *ttl_expired_live /",
      "*rt_cross_gen). They are post-major-compaction (nb-3-big) fixtures and still",
      "present the physical-vs-semantic divergence (a physical dump enumerates the",
      "surviving row/RT/expired markers that a SELECT hides).",
      "",
      "GAP (issue #1742 AC2): a dedicated SINGLE-generation (pre-compaction) fixture",
      "carrying coexisting partition-deletion + older LIVE rows, a range tombstone",
      "with a LIVE row INSIDE its bounds, and an expired TTL cell whose VALUE is",
      "still on disk requires generating uncompacted SSTables from a live Cassandra",
      "container (autocompaction disabled, single flush). Docker/Cassandra was not",
      "available in the implementing sandbox, so no such fixture is added here.",
      "Tracked for real-Cassandra regeneration in the issue #1742 follow-up."
    ]
  },
  "cases": [
    {
      "id": "static_with_rows__select_star",
      "keyspace": "test_deltas",
      "table": "static_with_rows",
      "fixture_dir_prefix": "static_with_rows-",
      "sstable_prefix": "nb-1-big",
      "schema": "deltas.cql",
      "ddl": "CREATE TABLE test_deltas.static_with_rows (pk int, ck int, static_col text static, row_col text, PRIMARY KEY (pk, ck))",
      "query": "SELECT pk, ck, static_col, row_col FROM test_deltas.static_with_rows",
      "reconciliation": ["static_columns"],
      "pinned_now_secs": 1800000000,
      "pinned_now_note": "2027-01-15T08:00:00Z — after every write in the fixture. It carries no TTL, tombstone or deletion of any kind, so the pin only removes wall-clock nondeterminism (#2642); it never selects between rows.",
      "physical_row_count": 12,
      "physical_static_only_partitions": 1,
      "note": [
        "Issue #3095 (STATIC-column SELECT semantics). THE case a physical dump",
        "structurally cannot express, in both directions:",
        "",
        "  * a `static_block` in the golden is NOT a result row, yet its value must",
        "    appear on EVERY clustering row of its partition — so a physical-dump",
        "    comparison passes while `SELECT` returns a phantom `ck = null` row per",
        "    static-bearing partition AND a null static column on every real row",
        "    (exactly the defect #3095 found: 16 rows where Cassandra returns 13);",
        "  * conversely a partition holding ONLY a static row (`pk = 99` here: a",
        "    `static_block` and no `row` entry) has ZERO physical rows but returns",
        "    ONE result row — so `physical_static_only_partitions` (1) is the amount",
        "    by which the semantic result LEGITIMATELY exceeds `physical_row_count`.",
        "    Both lanes re-derive that count from the committed golden and assert it,",
        "    so it can never be inflated to mask an over-emitting read path.",
        "",
        "Oracle derivation — Cassandra 5.0.8 `SelectStatement.processPartition()`:",
        "the static row is fetched OUT OF BAND via `partition.staticRow()`",
        "(`db/rows/BaseRowIterator.java`), so `partition.hasNext()` counts clustering",
        "rows only; with N > 0 clustering rows the loop emits N rows and fills every",
        "`case STATIC:` slot from that partition-level static row; with ZERO",
        "clustering rows it emits exactly one row (clustering + REGULAR columns null",
        "via `default: result.add((ByteBuffer) null)`) and returns.",
        "",
        "Fixture: `test_deltas.static_with_rows` is a real Apache Cassandra 5.0.2",
        "container flush (`test-data/scripts/generate-deltas.sh`, autocompaction",
        "disabled, single `nodetool flush`) with a COMMITTED `Data.db` + golden. A",
        "CQLite-written round trip could not serve as the oracle here: it is",
        "invariant to a uniform assembly error and is confounded by the write-side",
        "#1074 (statics placed into the clustering row) — see #3042."
      ],
      "expected_rows": [
        { "pk": 1, "ck": 1, "static_col": "static_val_1", "row_col": "row_1_1" },
        { "pk": 1, "ck": 2, "static_col": "static_val_1", "row_col": "row_1_2" },
        { "pk": 1, "ck": 3, "static_col": "static_val_1", "row_col": "row_1_3" },
        { "pk": 1, "ck": 4, "static_col": "static_val_1", "row_col": "row_1_4" },
        { "pk": 2, "ck": 1, "static_col": "static_val_2", "row_col": "row_2_1" },
        { "pk": 2, "ck": 2, "static_col": "static_val_2", "row_col": "row_2_2" },
        { "pk": 2, "ck": 3, "static_col": "static_val_2", "row_col": "row_2_3" },
        { "pk": 2, "ck": 4, "static_col": "static_val_2", "row_col": "row_2_4" },
        { "pk": 3, "ck": 1, "static_col": "static_val_3", "row_col": "row_3_1" },
        { "pk": 3, "ck": 2, "static_col": "static_val_3", "row_col": "row_3_2" },
        { "pk": 3, "ck": 3, "static_col": "static_val_3", "row_col": "row_3_3" },
        { "pk": 3, "ck": 4, "static_col": "static_val_3", "row_col": "row_3_4" },
        { "pk": 99, "ck": null, "static_col": "static_only_val", "row_col": null }
      ]
    },
    {
      "id": "ttl_expired_live__select_star",
      "keyspace": "test_compaction_tombstone_ttl",
      "table": "ttl_expired_live",
      "fixture_dir_prefix": "ttl_expired_live-",
      "sstable_prefix": "nb-3-big",
      "schema": "compaction-tombstone-ttl-parity.cql",
      "query": "SELECT id, ck, v FROM test_compaction_tombstone_ttl.ttl_expired_live",
      "reconciliation": ["ttl_expiry"],
      "pinned_now_secs": 1782950400,
      "pinned_now_note": "2026-07-02T00:00:00Z — AFTER row ck=1's TTL expiry (expires_at 2026-07-01T23:54:23Z, epoch 1782950063); row ck=2 has no TTL so it stays live at any now.",
      "physical_row_count": 2,
      "expected_rows": [
        { "id": 1, "ck": 2, "v": "b-1-2" }
      ]
    },
    {
      "id": "shadow_row_delete__select_star",
      "keyspace": "test_compaction_tombstone_ttl",
      "table": "shadow_row_delete",
      "fixture_dir_prefix": "shadow_row_delete-",
      "sstable_prefix": "nb-3-big",
      "schema": "compaction-tombstone-ttl-parity.cql",
      "query": "SELECT id, ck, v FROM test_compaction_tombstone_ttl.shadow_row_delete",
      "reconciliation": ["row_deletion"],
      "pinned_now_secs": 1782950400,
      "pinned_now_note": "No TTL in this fixture; pin is fixed for determinism only. Row (id=1, ck=1) is a ROW tombstone (no live cells) and must be hidden.",
      "physical_row_count": 4,
      "expected_rows": [
        { "id": 1, "ck": 2, "v": "a-1-2" },
        { "id": 1, "ck": 3, "v": "b-1-3" },
        { "id": 2, "ck": 0, "v": "a-2-0" }
      ]
    },
    {
      "id": "rt_cross_gen__select_star",
      "keyspace": "test_compaction_tombstone_ttl",
      "table": "rt_cross_gen",
      "fixture_dir_prefix": "rt_cross_gen-",
      "sstable_prefix": "nb-3-big",
      "schema": "compaction-tombstone-ttl-parity.cql",
      "query": "SELECT id, ck, v FROM test_compaction_tombstone_ttl.rt_cross_gen",
      "reconciliation": ["range_tombstone"],
      "pinned_now_secs": 1782950400,
      "pinned_now_note": "No TTL; pin fixed for determinism. A range tombstone covers clustering [10,25]; the live rows sit at ck=5 and ck=30 (OUTSIDE the RT), so both survive — this pins that the RT open/close markers are skipped and do not wrongly shadow rows outside their bounds. A LIVE-row-INSIDE-RT case needs the single-gen fixture noted in provenance.gap.",
      "physical_row_count": 2,
      "expected_rows": [
        { "id": 1, "ck": 5, "v": "a-1-5" },
        { "id": 1, "ck": 30, "v": "b-1-30" }
      ]
    },
    {
      "id": "wide_table_bti__clustering_slice",
      "keyspace": "test_da",
      "table": "wide_table",
      "fixture_dir_prefix": "wide_table-",
      "sstable_prefix": "da-2-bti",
      "schema": "wide-table-bti.cql",
      "query": "SELECT pk, ck FROM test_da.wide_table WHERE pk = 1 AND ck >= 100 AND ck < 110",
      "reconciliation": ["none_all_live"],
      "pinned_now_secs": 1800000000,
      "pinned_now_note": "2027-01-15T08:00:00Z. This fixture carries no TTL, tombstone or deletion of any kind (every row is unconditionally live at any now), so the pin only removes wall-clock nondeterminism from the lane; it never selects between rows.",
      "physical_row_count": 900,
      "note": [
        "Issue #3002 (BTI Rows.db row-index root base + leading OSS50 NEXT_COMPONENT).",
        "A BEHAVIOUR-PRESERVATION oracle for the BTI clustering-slice read path",
        "(`scan_single_partition_clustering` -> `resolve_clustering_seek_window` ->",
        "`bti_clustering_row_window`): the two compensating defects cancelled on this",
        "fixture, so the semantic result set must be IDENTICAL before and after removing",
        "the compensation. A change here is a regression, not a semantic update.",
        "",
        "Oracle derivation: test_da/wide_table is a real Apache Cassandra 5.0 `da` (BTI)",
        "SSTable whose committed sstabledump golden (da-2-bti-Data.db.jsonl) holds 900",
        "physical rows (3 partitions x ck=0..299) with NO tombstone/TTL/deletion marker,",
        "so physical == semantic here: the expected result set is exactly the golden's",
        "pk=1 rows with 100 <= ck < 110, read off the golden rather than hand-authored.",
        "",
        "flight_lane=false: the Flight `do_get` ticket surface is a whole-table projection",
        "scan with no WHERE clause, so a partition + clustering-slice query is not",
        "expressible on that lane (see cqlite-flight/tests/query_semantics_flight_parity.rs)."
      ],
      "flight_lane": false,
      "expected_rows": [
        { "pk": 1, "ck": 100 },
        { "pk": 1, "ck": 101 },
        { "pk": 1, "ck": 102 },
        { "pk": 1, "ck": 103 },
        { "pk": 1, "ck": 104 },
        { "pk": 1, "ck": 105 },
        { "pk": 1, "ck": 106 },
        { "pk": 1, "ck": 107 },
        { "pk": 1, "ck": 108 },
        { "pk": 1, "ck": 109 }
      ]
    },
    {
      "id": "static_with_tombstones__deleted_cell_null",
      "keyspace": "test_tomb",
      "table": "static_with_tombstones",
      "fixture_dir_prefix": "static_with_tombstones-",
      "sstable_prefix": "nb-1-big",
      "schema": "tombstone-parity.cql",
      "query": "SELECT pk, ck, row_col, stat_col FROM test_tomb.static_with_tombstones WHERE pk = 1",
      "reconciliation": ["cell_tombstone", "row_deletion", "range_tombstone"],
      "pinned_now_secs": 1782950400,
      "pinned_now_note": "2026-07-02T00:00:00Z (the same pin the sibling cases use). This fixture carries NO TTL of any kind, so no row's visibility depends on `now` \u2014 the pin only removes wall-clock nondeterminism. The tombstones' localDeletionTimes (2026-06-24T22:59:06Z) govern GC purge, never read visibility, so they do not interact with the pin either.",
      "physical_row_count": 4,
      "note": [
        "Issue #3094: a DELETED CELL must read as NULL, never surface a raw tombstone.",
        "",
        "THE CELL-TOMBSTONE SHAPE ON CASSANDRA-WRITTEN BYTES. test_tomb",
        "static_with_tombstones is a real Apache Cassandra 5.0 `nb` SSTable (single",
        "flush), committed to the repo with provenance in",
        "test-data/cassandra-parity-manifest.yml. Its committed sstabledump golden",
        "(nb-1-big-Data.db.jsonl) enumerates, in partition pk=1:",
        "  * a STATIC BLOCK holding ONE live cell, stat_col='surviving_static'",
        "           (tstamp 2021-01-01, no deletion_info, no TTL)",
        "  * ck=1 - live liveness marker + live row_col='row_1'",
        "  * ck=2 - a ROW DELETION (marked_deleted 2021-01-02), zero cells",
        "  * ck=3 - a LIVE liveness marker (tstamp 2021-01-01) whose ONLY regular",
        "           cell, row_col, is a SIMPLE CELL TOMBSTONE (tstamp 2021-01-02)",
        "  * a RANGE TOMBSTONE bound pair covering [ck=4, ck=5] (marked_deleted",
        "           2021-01-02); no data rows lie inside it in this fixture",
        "  * ck=6 - live liveness marker + live row_col='row_6'",
        "",
        "CASSANDRA 5.0.8 DERIVATION of the expected result set, per `ck` (every",
        "expectation below is read off the golden above plus the pinned",
        "cassandra-5.0.8 sources named here - never off CQLite's own output). The",
        "client-visible result is what `db/transform/Filter.java` yields:",
        "applyToRow() = `row.purge(DeletionPurger.PURGE_ALL, nowInSec,",
        "enforceStrictLiveness)`, and applyToMarker() returns null.",
        "  * ck=1 -> RETURNED, row_col='row_1'. No tombstone and no expiring cell,",
        "           so BTreeRow.purge()'s `!hasDeletion(nowInSec)` short-circuit",
        "           returns the row unchanged.",
        "  * ck=2 -> ABSENT. Its primaryKeyLivenessInfo is EMPTY (the golden shows",
        "           deletion_info and no liveness_info) and it carries zero cells;",
        "           PURGE_ALL strips the row deletion to Deletion.LIVE, so",
        "           BTreeRow.update()'s `info.isEmpty() && deletion.isLive() &&",
        "           BTree.isEmpty(newTree)` returns NULL and the row is dropped.",
        "           This holds independently of enforceStrictLiveness.",
        "  * ck=3 -> RETURNED with row_col NULL. Its liveness marker is live (no",
        "           TTL), so shouldPurge(LivenessInfo, nowInSec) is false and the",
        "           marker is kept; its only cell is a tombstone, which",
        "           AbstractCell.purge() maps to null. update() therefore does NOT",
        "           return null (info is non-empty) and yields a row with zero",
        "           cells, which SelectStatement.processPartition() renders as",
        "           `result.add(row.getColumnData(def) /* null */, nowInSec)` = NULL.",
        "  * ck=4, ck=5 -> ABSENT. The generator inserted them at T_GEN1 and range-",
        "           deleted them at T_GEN2 before the single flush, so the memtable",
        "           merge left NO `row` entry on disk - only the RangeTombstone",
        "           bound pair, and Filter.applyToMarker() returns null, so markers",
        "           are never client-visible.",
        "  * ck=6 -> RETURNED, row_col='row_6' (same short-circuit as ck=1).",
        "  * stat_col='surviving_static' on ALL THREE returned rows, ck=3",
        "           INCLUDED. processPartition() captures `Row staticRow =",
        "           partition.staticRow()` ONCE per partition and, inside its",
        "           per-row loop, serves `case STATIC: result.add(",
        "           staticRow.getColumnData(def), nowInSec)` - so the static row",
        "           is a SEPARATE object attached to every emitted clustering row,",
        "           never read from that row's own (here purged) cells. The static",
        "           row itself survives Filter.applyToStatic(): it is non-empty,",
        "           and purge()'s `!hasDeletion(nowInSec)` short-circuit returns it",
        "           unchanged because its one cell is live.",
        "So the SELECT returns 3 rows (ck = 1, 3, 6), one of them with",
        "row_col = null and all three with stat_col = 'surviving_static', while",
        "the physical dump shows 4 `type: row` entries.",
        "",
        "This is exactly the divergence a physical-dump oracle CANNOT catch: the",
        "golden enumerates the cell tombstone, so parity against it stays green",
        "while a real SELECT diverges. Before #3094 the deleted cell reached the row",
        "carrier as Value::Tombstone(CellTombstone) and the fail-closed Arrow",
        "encoder (#1485) errored the WHOLE Flight do_get stream.",
        "",
        "SCOPE OF THE QUERY (why the FULL `WHERE pk = 1` slice, and why stat_col",
        "IS now projected). Issue #3164 RETIRED the `ck >= 3` bound this case",
        "originally carried. When #3094 added the case, the row-tombstoned ck=2 was",
        "wrongly surfacing as an all-null row: on the single-generation read path",
        "`merge_static_cells` injected the live stat_col into ck=2 BEFORE",
        "`build_display_row` ran, so `has_data_cell` was true and the row was",
        "emitted as data instead of a filtered tombstone MARKER. The bound existed",
        "so this case could own the CELL-TOMBSTONE property alone and never encode",
        "that phantom row as expected behaviour. That mechanism is FIXED - statics",
        "are excluded from the row-tombstone decision (PR #3141, issue #3095) and",
        "the rule is pinned at all three gated decode sites by PR #3159 (issue",
        "#3121, cqlite-core/tests/issue_3121_static_row_tombstone_no_phantom.rs) -",
        "so the bound is now pure under-coverage and the case asserts the whole",
        "partition, ck=2's ABSENCE included. Should ck=2 ever reappear as an",
        "all-null row, those three tests are the ones to consult first.",
        "stat_col is PROJECTED, which is the point of a case named",
        "static_with_tombstones: it asserts that the live static cell attaches to",
        "every surviving row of a tombstone-riddled partition - ck=3 included,",
        "where the row's own regular cell was purged. #3164 considered omitting it",
        "on the grounds that the static read shape is a tracked divergence, but",
        "BypassReason::StaticColumns lives in cqlite-flight/src/bypass.rs and is a",
        "property of the FLIGHT single-SSTable bypass predicate; nothing in",
        "cqlite-core consults it, and this case is flight_lane=false, so it does",
        "not gate this lane. The slice keeps TWO live control rows (ck=1,",
        "row_col='row_1' and ck=6, row_col='row_6') in the same result set, so the",
        "ck=3 null cannot pass vacuously as a blanket loss of row_col, and stat_col",
        "is asserted non-null on all three so a blanket loss of the static cell",
        "cannot pass either.",
        "",
        "flight_lane=false: the Flight `do_get` ticket surface is a whole-table",
        "projection scan with no WHERE clause, so even this partition-restricted",
        "slice is not expressible on that lane.",
        "",
        "WHAT THE FLIGHT LANE DOES AND DOES NOT COVER for this shape (issue",
        "#3042). Two Flight tests exercise cell-tombstone -> NULL on BOTH forced",
        "arms - cqlite-flight/tests/issue_3094_cell_tombstone_null.rs, and the ck=5",
        "cell-tombstone shape in cqlite-flight/tests/",
        "issue_3058_forced_path_differential.rs - but BOTH run on CQLITE-WRITTEN",
        "fixtures (write_engine CellOperation::Write/Delete in",
        "differential_fixtures/mod.rs), NOT on this Cassandra `nb` fixture. They",
        "are a legitimate oracle for the ONE property they claim, because",
        "cell-tombstone -> NULL is a read-time RECONCILIATION property rather than",
        "an on-disk FRAMING property (#3042's caveat bites the latter); but a",
        "CQLite-written/CQLite-read test can never SUBSTITUTE for a",
        "Cassandra-fixture oracle, and neither of them covers either property this",
        "case newly added in #3164: the ABSENCE of the row-tombstoned ck=2, and the",
        "attachment of the live static cell to every surviving row. So a",
        "Cassandra-fixture Flight arm for this shape DOES NOT EXIST yet - recorded",
        "as owed, not silently claimed. It is also not a small addition: the Flight",
        "fast arm currently fails CLOSED on exactly this fixture",
        "(BypassReason::StaticColumns / #3140 in cqlite-flight/src/bypass.rs,",
        "because the single-generation arm surfaces ck=3's cell tombstone as a raw",
        "Value::Tombstone), so the arm has to be fixed before it can be asserted."
      ],
      "flight_lane": false,
      "expected_rows": [
        { "pk": 1, "ck": 1, "row_col": "row_1", "stat_col": "surviving_static" },
        { "pk": 1, "ck": 3, "row_col": null, "stat_col": "surviving_static" },
        { "pk": 1, "ck": 6, "row_col": "row_6", "stat_col": "surviving_static" }
      ]
    },
    {
      "id": "multiclustering_bti__multi_component_slice",
      "keyspace": "test_da",
      "table": "multiclustering_table",
      "fixture_dir_prefix": "multiclustering_table-",
      "sstable_prefix": "da-2-bti",
      "schema": "multiclustering-table-bti.cql",
      "ddl": "CREATE TABLE test_da.multiclustering_table (pk int, bucket text, seq int, payload text, PRIMARY KEY (pk, bucket, seq))",
      "query": "SELECT pk, bucket, seq FROM test_da.multiclustering_table WHERE pk = 1 AND bucket = 'bo' AND seq >= 10 AND seq < 20",
      "reconciliation": ["none_all_live"],
      "pinned_now_secs": 1800000000,
      "pinned_now_note": "2027-01-15T08:00:00Z. This fixture carries no TTL, tombstone or deletion of any kind (every entry in the committed golden is a live `row` with a plain liveness_info, and no partition or row carries a deletion_info), so the pin only removes wall-clock nondeterminism from the lane; it never selects between rows.",
      "physical_row_count": 468,
      "note": [
        "A clustering slice bounded on MORE THAN the first clustering component: an equality on",
        "`bucket` plus a half-open range on `seq`. test_da/wide_table (a single `int` clustering",
        "column) structurally cannot express this shape, which is why this fixture exists.",
        "",
        "Issue #3032 (compound-clustering BTI fixture). test_da/multiclustering_table is a real",
        "Apache Cassandra 5.0.2 `da` (BTI) SSTable with PRIMARY KEY (pk, bucket, seq) - two",
        "clustering components of DIFFERING types (text then int) - and 3 partitions of",
        "DELIBERATELY non-uniform shape (pk=1: 3 buckets x 60 rows; pk=2: 5 x 32; pk=3: 8 x 16;",
        "468 rows total).",
        "",
        "Oracle derivation: the committed sstabledump golden (da-2-bti-Data.db.jsonl) holds 468",
        "physical rows with NO tombstone/TTL/deletion marker anywhere, so physical == semantic",
        "here - expected_rows is exactly the golden's matching (pk, bucket, seq) triples, READ OFF",
        "the golden rather than hand-authored.",
        "",
        "flight_lane=false: the Flight `do_get` ticket surface is a whole-table projection scan",
        "with no WHERE clause, so a partition + clustering-slice query is not expressible on that",
        "lane (see cqlite-flight/tests/query_semantics_flight_parity.rs)."
      ],
      "flight_lane": false,
      "expected_rows": [
        { "pk": 1, "bucket": "bo", "seq": 10 },
        { "pk": 1, "bucket": "bo", "seq": 11 },
        { "pk": 1, "bucket": "bo", "seq": 12 },
        { "pk": 1, "bucket": "bo", "seq": 13 },
        { "pk": 1, "bucket": "bo", "seq": 14 },
        { "pk": 1, "bucket": "bo", "seq": 15 },
        { "pk": 1, "bucket": "bo", "seq": 16 },
        { "pk": 1, "bucket": "bo", "seq": 17 },
        { "pk": 1, "bucket": "bo", "seq": 18 },
        { "pk": 1, "bucket": "bo", "seq": 19 }
      ]
    },
    {
      "id": "multiclustering_bti__first_component_prefix_slice",
      "keyspace": "test_da",
      "table": "multiclustering_table",
      "fixture_dir_prefix": "multiclustering_table-",
      "sstable_prefix": "da-2-bti",
      "schema": "multiclustering-table-bti.cql",
      "ddl": "CREATE TABLE test_da.multiclustering_table (pk int, bucket text, seq int, payload text, PRIMARY KEY (pk, bucket, seq))",
      "query": "SELECT pk, bucket, seq FROM test_da.multiclustering_table WHERE pk = 3 AND bucket = 'golf'",
      "reconciliation": ["none_all_live"],
      "pinned_now_secs": 1800000000,
      "pinned_now_note": "2027-01-15T08:00:00Z. This fixture carries no TTL, tombstone or deletion of any kind (every entry in the committed golden is a live `row` with a plain liveness_info, and no partition or row carries a deletion_info), so the pin only removes wall-clock nondeterminism from the lane; it never selects between rows.",
      "physical_row_count": 468,
      "note": [
        "A bound naming only a PROPER PREFIX of the clustering key (`bucket`, not `seq`) - the",
        "shape that drives the BTI Rows.db prefix-bound narrowing. It is the end-to-end regression",
        "oracle for the two byte-comparable encoding defects issue #3032 uncovered: the OSS50",
        "variable-length component terminator (a single ByteSource.ESCAPE 0x00, never 00 FF) and",
        "Cassandra's ClusteringPrefix.Kind bound terminators (LT/GT_NEXT_COMPONENT). With either",
        "defect present this query returned a TRUNCATED 7 of 16 rows.",
        "",
        "Issue #3032 (compound-clustering BTI fixture). test_da/multiclustering_table is a real",
        "Apache Cassandra 5.0.2 `da` (BTI) SSTable with PRIMARY KEY (pk, bucket, seq) - two",
        "clustering components of DIFFERING types (text then int) - and 3 partitions of",
        "DELIBERATELY non-uniform shape (pk=1: 3 buckets x 60 rows; pk=2: 5 x 32; pk=3: 8 x 16;",
        "468 rows total).",
        "",
        "Oracle derivation: the committed sstabledump golden (da-2-bti-Data.db.jsonl) holds 468",
        "physical rows with NO tombstone/TTL/deletion marker anywhere, so physical == semantic",
        "here - expected_rows is exactly the golden's matching (pk, bucket, seq) triples, READ OFF",
        "the golden rather than hand-authored.",
        "",
        "flight_lane=false: the Flight `do_get` ticket surface is a whole-table projection scan",
        "with no WHERE clause, so a partition + clustering-slice query is not expressible on that",
        "lane (see cqlite-flight/tests/query_semantics_flight_parity.rs)."
      ],
      "flight_lane": false,
      "expected_rows": [
        { "pk": 3, "bucket": "golf", "seq": 0 },
        { "pk": 3, "bucket": "golf", "seq": 1 },
        { "pk": 3, "bucket": "golf", "seq": 2 },
        { "pk": 3, "bucket": "golf", "seq": 3 },
        { "pk": 3, "bucket": "golf", "seq": 4 },
        { "pk": 3, "bucket": "golf", "seq": 5 },
        { "pk": 3, "bucket": "golf", "seq": 6 },
        { "pk": 3, "bucket": "golf", "seq": 7 },
        { "pk": 3, "bucket": "golf", "seq": 8 },
        { "pk": 3, "bucket": "golf", "seq": 9 },
        { "pk": 3, "bucket": "golf", "seq": 10 },
        { "pk": 3, "bucket": "golf", "seq": 11 },
        { "pk": 3, "bucket": "golf", "seq": 12 },
        { "pk": 3, "bucket": "golf", "seq": 13 },
        { "pk": 3, "bucket": "golf", "seq": 14 },
        { "pk": 3, "bucket": "golf", "seq": 15 }
      ]
    },
    {
      "id": "ttl_table_bti__full_scan_live",
      "keyspace": "test_da",
      "table": "ttl_table",
      "fixture_dir_prefix": "ttl_table-",
      "sstable_prefix": "da-2-bti",
      "schema": "da-test.cql",
      "ddl": "CREATE TABLE test_da.ttl_table (id uuid PRIMARY KEY, data text, expiring_value int)",
      "query": "SELECT data, expiring_value FROM test_da.ttl_table",
      "reconciliation": ["ttl_not_yet_expired_at_pinned_now"],
      "pinned_now_secs": 1781136000,
      "pinned_now_note": "2026-06-11T00:00:00Z — BEFORE both rows' TTL expiry (expires_at 2026-06-11T16:17:37Z, epoch 1781194657) and after both writes (tstamp 2026-06-10T16:17:37Z), so every row is LIVE. This is the non-vacuous half of the pair: with the sibling `__full_scan_expired` case it pins that the pin SELECTS between rows rather than the reader returning a constant.",
      "physical_row_count": 2,
      "note": [
        "Issue #3109: the FIRST oracle case that drives a BTI (`da`) table through the",
        "BATCHED streaming surface. Every other test_da case carries a WHERE clause and so",
        "takes the partition-targeted lookup path; a WHERE-less SELECT falls through to the",
        "full-scan streaming path, which consumes `scan_stream_batched`",
        "(query/select_executor/streaming.rs, query/executor.rs) -> SSTableManager's",
        "single-reader fast path -> SSTableReader::scan_stream_batched.",
        "",
        "That surface had NO `bti_partitions_db.is_some()` dispatch, so a `da` reader decoded",
        "through the V5UncompressedOA state machine, which honours neither read_shadowing nor",
        "a caller-pinned now_secs. Both halves of this pair therefore exercise exactly the",
        "posture #3109 restored: this one that a pinned now BEFORE expiry keeps rows,",
        "`__full_scan_expired` that a pinned now AFTER expiry hides them.",
        "",
        "Oracle derivation: the committed sstabledump golden (da-2-bti-Data.db.jsonl) records",
        "both rows with `\"ttl\": 86400` and `\"expires_at\": \"2026-06-11T16:17:37Z\"`; the",
        "expected values are read straight off that golden's cells. Projection excludes the",
        "uuid partition key deliberately — both lanes' scalar renderers cover int/text only.",
        "",
        "flight_lane: default (true) — a WHERE-less whole-table projection IS expressible on",
        "the Flight ticket surface, so this case runs on both lanes."
      ],
      "expected_rows": [
        { "data": "BTI TTL row 1", "expiring_value": 42 },
        { "data": "BTI TTL row 2", "expiring_value": 99 }
      ]
    },
    {
      "id": "ttl_table_bti__full_scan_expired",
      "keyspace": "test_da",
      "table": "ttl_table",
      "fixture_dir_prefix": "ttl_table-",
      "sstable_prefix": "da-2-bti",
      "schema": "da-test.cql",
      "ddl": "CREATE TABLE test_da.ttl_table (id uuid PRIMARY KEY, data text, expiring_value int)",
      "query": "SELECT data, expiring_value FROM test_da.ttl_table",
      "reconciliation": ["ttl_expired_at_pinned_now"],
      "pinned_now_secs": 1782950400,
      "pinned_now_note": "2026-07-02T00:00:00Z (the same pin the compaction-tombstone-ttl cases use) — AFTER both rows' TTL expiry (expires_at 2026-06-11T16:17:37Z, epoch 1781194657), so Cassandra's SELECT returns NOTHING while the physical dump still shows both rows.",
      "physical_row_count": 2,
      "expect_empty": true,
      "note": [
        "Issue #3109, the shadowing half of the BTI-through-the-batched-surface pair. This is",
        "the case the bug FAILED: with no BTI dispatch on `run_scan_stream_batched` the `da`",
        "reader decoded through the state machine, which drops `read_shadowing`, so both",
        "TTL-expired rows were surfaced where `scan` and the per-row stream hide them.",
        "",
        "`expect_empty` is legitimate here and provable: physical_row_count is 2 (re-derived",
        "from the committed golden by the harness), so the rows demonstrably exist on disk and",
        "are reconciled away at read time — not an empty or misconfigured fixture. Its sibling",
        "`__full_scan_live` runs the SAME query at a pin BEFORE the expiry and requires both",
        "rows, so neither case can pass by a reader that always returns a constant."
      ],
      "expected_rows": []
    }
  ]
}
