== #3287 capability probe — RECORDED READINGS ==
SCOPE: this probe RECORDS and VALIDATES; it does not classify (#3870).
       No line below is a capability verdict. Gates A-D are answered by
       READING these numbers — see negative-control-c7i-guest.md for this
       host's reading. VERDICT at the foot covers data integrity ONLY.
host L3: 105 MiB (1 instance)
cpuset:  0,8 (complete sibling group from cpu0, all members online AND in this process's Cpus_allowed_list)
window:  control-FIFO gated to the chase; events counted :u; small groups, 100.00% enabled REQUIRED
gate-integrity: MEASURED-OK — instruction scaling 1e3->1e5 = 95.35x (need >=10x, ideal ~100x)
  absolute, REPORTED not asserted: 6296 instructions for 1e3 accesses
  (#3224 used a tuned 1e6 ceiling; the scaling property is host-independent.)

-- GATE A: TMA (raw record; #3287 AC1 turns on what tma-probe.txt shows) --
  perf stat -M TopdownL1 : rc=1, 4 line(s) recorded verbatim in tma-probe.txt (this probe does not interpret it)
  perf stat -M TopdownL2 : rc=1, 4 line(s) recorded verbatim in tma-probe.txt (this probe does not interpret it)
  READ tma-probe.txt: a metric group that produced real level-2 metric rows
  with numeric shares answers AC1 yes; a not-found/not-supported diagnostic
  answers it no; anything else means the step must be re-run. This script
  deliberately does not make that call (#3870).

-- GATE B: offcore / prefetch-stall term (the one #3287 exists for) --
  (counts as measured; the 2 GiB arm is many times L3, so read a 0 there)
  offcore_requests_outstanding.all_data_rd:u : disposition=PROGRAMS  friendly-L2resident=0  hostile-512m=0  hostile-2g=0
  offcore_requests_outstanding.cycles_with_data_rd:u : disposition=PROGRAMS  friendly-L2resident=0  hostile-512m=0  hostile-2g=0
  offcore_requests.all_data_rd : ABSENT-FROM-PMU (disposition only; not in a differential group)
  offcore_requests_buffer.sq_full : ABSENT-FROM-PMU (disposition only; not in a differential group)
  l1d_pend_miss.fb_full:u : disposition=PROGRAMS  friendly-L2resident=1595137  hostile-512m=11591587  hostile-2g=8876391
  l1d_pend_miss.pending:u : disposition=PROGRAMS  friendly-L2resident=143431090  hostile-512m=11929334089  hostile-2g=7124705942

-- GATE C: reproduce #3224's own baseline counters --
  cycle_activity.stalls_l3_miss:u : disposition=PROGRAMS  friendly-L2resident=0  hostile-512m=0  hostile-2g=0
  cycle_activity.stalls_l2_miss:u : disposition=PROGRAMS  friendly-L2resident=3830697  hostile-512m=12100861830  hostile-2g=6831281729
  cycle_activity.stalls_total:u : disposition=PROGRAMS  friendly-L2resident=308941191  hostile-512m=13591506372  hostile-2g=7500452260
  LLC-loads:u : disposition=ABSENT-FROM-PMU  friendly-L2resident=(no value in this run's CSV)  hostile-512m=(no value in this run's CSV)  hostile-2g=(no value in this run's CSV)
  LLC-load-misses:u : disposition=ABSENT-FROM-PMU  friendly-L2resident=(no value in this run's CSV)  hostile-512m=(no value in this run's CSV)  hostile-2g=(no value in this run's CSV)
  cache-references:u : disposition=PROGRAMS  friendly-L2resident=0  hostile-512m=0  hostile-2g=0
  cache-misses:u : disposition=PROGRAMS  friendly-L2resident=0  hostile-512m=0  hostile-2g=0
  NESTING (stalls_l3_miss <= stalls_l2_miss <= stalls_total), per arm:
    DECLARED LIMIT: nesting HOLDS TRIVIALLY when stalls_l3_miss reads 0 in
    every arm, so a HOLDS here is NOT evidence that counter works — read its
    recorded counts above.
    This check catches a nesting VIOLATION (which would invalidate #3224's
    difference-based partition); it cannot catch a silent zero. Only the
    differential can, which is why both are reported.
    friendly-L2resident: HOLDS (0 <= 3830697 <= 308941191)
    hostile-512m: HOLDS (0 <= 12100861830 <= 13591506372)
    hostile-2g: HOLDS (0 <= 6831281729 <= 7500452260)

-- GATE D: topology (reported; #3224's geometry needs 6+2 complete cores EXCLUSIVELY) --
  see capability-probe.txt

-- events EXCLUDED from the differential, and why (never silently dropped) --
  cache:LLC-loads=ABSENT-FROM-PMU
  cache:LLC-load-misses=ABSENT-FROM-PMU

Prediction, written BEFORE measuring: the 2 GiB arm's working set is many
times L3 and is a random single-dependency chase, so it cannot be L3-resident.
Any honest L3-miss-stall or offcore counter MUST be large there. A zero is a
silent instrument -- and a zero is invariant to the window, since
contamination can only ADD counts.

VERDICT: COMPLETE (data integrity only — every step executed and every
         data-integrity guard passed. This is NOT a capability claim and
         NOT an answer to any of Gates A-D: this probe records, it does
         not classify (#3870). Read the numbers above.)
