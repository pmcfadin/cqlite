== #3287 capability probe ==
L3 as this host sees it: 105 MiB (1 instance)
window: control-FIFO gated to the chase (-D -1 --control fifo:), events counted :u
gate-integrity-probe: OK

Prediction BEFORE measuring: the 2048MiB arm's working set is many times L3,
a random single-dependency chase, so it cannot be L3-resident. Any honest
L3-miss-stall or offcore counter MUST be large there. A zero is a silent
instrument, not a measurement -- and a zero is invariant to the window,
since contamination can only ADD counts.

VERDICT: COMPLETE (all steps measured; read host/arm-*.csv for values)
